# Suggested commands
- `./spec-dock/scripts/spec-dock active show` : inspect the current active SpecDock context at session start.
- `./spec-dock/scripts/spec-dock validate` : run after structural changes and before handoff.
- `./spec-dock/scripts/spec-dock sync` : use only to regenerate CLI-managed state, not as a general repair tool.
- `spec-manager` : preferred command-first interface for SpecDock operations.
- NOTE: broader build/test/lint commands were not verified from project config during this session.